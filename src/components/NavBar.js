import React from "react";
import SearchBar from "./SearchBar";

const NavBar = () => {
  return (
    <div className="navbar">
      <h2>Song blog</h2>
      <SearchBar />
    </div>
  );
};

export default NavBar;
